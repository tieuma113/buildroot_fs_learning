# buildroot_fs_learning

```
buildroot-fs-learning/
├── README.md
├── .gitignore
├── configs/
│   └── defconfig              # defconfig tùy biến của bạn
├── apps/
│   └── my_app/                # App bạn viết
├── buildroot/                 # Submodule buildroot
├── output/                    # Output build (nên ignore trong git)
└── scripts/
    └── build.sh               # Script build toàn bộ (tùy chọn)
```